# Host PID Usage Exceeds Threshold (12027) Alarm Diagnosis Skill

## Diagnosis Flow
```
Step 1: Confirm alarm and instance status
  |
  +-- Instance normal -> Check monitoring metric trend
  +-- Instance abnormal -> Step 2
Step 2: Check host resource metrics
  |
  +-- Metric abnormal -> Resource bottleneck
  +-- Metric normal -> Step 3
```

## API Call Chain Dependencies
| Previous API | Return fields | Next API | Parameters |
|-----------|---------|-----------|------|
| `start_log_search` | `task_id` | `get_log_search_progress` | `search_id` |
| `get_instances` | Instance status | `get_host_process` | Confirm whether the process exists |

---

## Diagnosis Steps
### Step 1: Confirm alarm and instance status
```bash
# Query the current alarm list
python manager_api_client.py -a get_alarms --json
```

```bash
# Query the OMS instance status
python manager_api_client.py -a get_instances \
  -p 'cluster_id=<cluster_id>' \
  -p 'service_name=OMS' \
  --json
```

```bash
# Query the host process status
python manager_api_client.py -a get_host_process \
  -p 'hostname=<node_name>' \
  --json
```

**Analysis**:

| Actual result | Conclusion | Next step |
|----------|------|--------|
| Instance status normal | The alarm may have recovered or be a metric-based alarm | Step 2 |
| Instance status abnormal | Service abnormal | Step 2 |
| Correlated alarms exist | Handle the correlated alarm first | Refer to the corresponding alarm diagnosis |

---
### Step 2: Check host resource metrics
```bash
# Query host monitoring metrics (PID usage / process count)
python manager_api_client.py -a get_host_metrics \
  -p 'hostname=<node_name>' \
  -p 'metric_names=dev_pid_resource_usage,dev_process_total' \
  --json
```

```bash
# Query host resource information
python manager_api_client.py -a get_host_resource \
  -p 'hostname=<node_name>' \
  --json
```

**Analysis**:

| Actual result | Conclusion |
|----------|------|
| Metric abnormal | Process count is approaching the system limit, resource bottleneck causes the alarm |
| Metric normal | Continue log troubleshooting |

---
### Step 3: In-depth log troubleshooting

```bash
# Search for process count / handle related error keywords
python manager_api_client.py -a start_log_search \
  -p 'cluster_id=<cluster_id>' \
  -p 'key_word=Too many open files|fork|Cannot allocate' \
  -p 'start_time=<alarm_time>' \
  -p 'end_time=<current_time>' \
  -p 'services=Manager:Manager:Agent' \
  -p 'min_log_level=WARN' \
  -p 'log_file_name=nodeagent_ctl.log' \
  --json

# Check the search progress
python manager_api_client.py -a get_log_search_progress \
  -p 'search_id=<task_id>' \
  --json
```

**Keyword analysis**:

| Log keyword | Conclusion |
|-----------|------|
| Too many open files / fork / Cannot allocate | Process count or handle count exhausted; see Common Root Causes and Repair Suggestions for the corresponding root cause |
| None of the above keywords | No abnormality in this log; manually run `ps -eLf | wc -l` on the alarm node to confirm the total process count |

---

## Common Root Causes and Repair Suggestions

### 1. Resource metric exceeds threshold

**Repair suggestions**:
- Handle the specific over-threshold metric accordingly
- Clean up resources or expand capacity

### 2. Insufficient disk space

**Feature**: The log contains `no space left` / `No space left on device`

**Repair suggestions**:
- Clean up log files and temporary files
- Expand disk capacity

### 3. Configuration or file permission anomaly

**Feature**: The log contains `Permission denied` / `cannot open`

**Repair suggestions**:
- Check the permissions of the related files
- Wait 5 minutes after fixing the permissions

### 4. Network or connectivity issue

**Feature**: The log contains `connection refused` / `timeout`

**Repair suggestions**:
- Check network connectivity between nodes
- Use the `check_remote` API to test the reachability of the remote node

## Variable Description

| Variable | Description | Example |
|------|------|------|
| `<cluster_id>` | MRS cluster identifier | fd04c789-39d4-4847-8fc9-4572fec9414f |
| `<node_name>` | Alarm node name | 8-5-235-6 |
| `<alarm_time>` | Alarm trigger time | 2026/09/02 10:00:00 GMT+08:00 |
| `<current_time>` | Current time | 2026/09/02 11:00:00 GMT+08:00 |
| `<task_id>` | Log search task ID | Obtained from the `start_log_search` return value |
