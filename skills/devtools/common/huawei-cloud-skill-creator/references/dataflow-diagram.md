# Data Flow Diagram — huawei-cloud-skill-creator

> Auto-generated by `scripts/generate-dataflow-diagram.sh` | Version: 1.0.0

## Workflow Data Flow

```mermaid
flowchart TD
  INPUT([/"User: Create a Huawei Cloud Skill"/])
  STEP1["Step 1: Requirements Analysis"]
  STEP2["Step 2: Naming & Directory"]
  STEP3["Step 3: API Research"]
  STEP4["Step 4: Data Flow Diagram"]
  STEP5["Step 5: Generate SKILL.md"]
  STEP6["Step 6: Generate references/"]
  STEP7["Step 7: Generate scripts/"]
  STEP8["Step 8: Generate templates/ & demo/"]
  STEP9["Step 9: Quality Validation"]
  OUTPUT([/"Complete Skill Package"/])

  subgraph CLI_OPS["CLI Operations"]
    CLI_OP1["hcloud {Service} --help"]
    CLI_OP2["hcloud {Service} List{Resources}"]
    CLI_OP3["hcloud {Service} {Operation} --help"]
  end

  subgraph DATA["Data Sources"]
    ENV[/"Environment Variables\nHUAWEI_ACCESS_KEY, HUAWEI_SECRET_KEY, HUAWEI_REGION"/]
    REFS["references/\nskill-spec-generic.md, naming-conventions.md"]
    TEMPLATES["templates/\nSKILL.md.template, dataflow-diagram.md.template"]
    SCRIPTS["scripts/\ngenerate-dataflow-diagram.sh, validate-skill.sh"]
  end

  INPUT --> STEP1
  STEP1 --> STEP2
  STEP2 --> STEP3
  STEP3 --> STEP4
  STEP4 --> STEP5
  STEP5 --> STEP6
  STEP6 --> STEP7
  STEP7 --> STEP8
  STEP8 --> STEP9
  STEP9 --> OUTPUT

  ENV -.-> STEP1
  REFS -.-> STEP2
  TEMPLATES -.-> STEP5
  SCRIPTS -.-> STEP4
  SCRIPTS -.-> STEP9

  STEP3 --> CLI_OPS
  CLI_OP1 -.-> STEP3
  CLI_OP2 -.-> STEP3
  CLI_OP3 -.-> STEP5
```

## Diagram Legend

| Symbol | Meaning |
|--------|---------|
| `([/ /])` | Start / End (asymmetric rectangle) |
| `[ ]` | Process step (rectangle) |
| `[/ /]` | Data store (parallelogram) |
| `-->` | Primary data flow |
| `-.->` | Secondary / reference data flow |

## Data Flow Description

| Step | Input | Process | Output |
|------|-------|---------|--------|
| 1. Requirements Analysis | User request + env config | Confirm service, scope, CLI commands, triggers | Structured requirements |
| 2. Naming & Directory | Requirements + naming conventions | Generate name via formula, create directory structure | Skill directory path |
| 3. API Research | Directory + CLI access | Discover operations via --help, test read-only commands | API operation list |
| 4. Data Flow Diagram | Workflow steps + CLI operations | Generate Mermaid diagram from template | `references/dataflow-diagram.md` |
| 5. Generate SKILL.md | API list + templates | Fill SKILL.md.template with service data | `SKILL.md` with Frontmatter + body |
| 6. Generate references/ | SKILL.md + API data | Create iam-policies, cli-guide, verification, etc. | `references/` directory |
| 7. Generate scripts/ | Workflow requirements | Create analysis/deployment/utility scripts | `scripts/` directory |
| 8. Generate templates/ & demo/ | Config patterns | Create IaC/API templates + example data | `templates/` + `demo/` |
| 9. Quality Validation | Complete skill directory | Run validate-skill.sh, check all gates | Validation report (PASS/FAIL) |
