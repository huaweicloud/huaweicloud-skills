{
  "region": "cn-north-4",
  "test_cases": [
    {
      "id": "TC-01",
      "name": "ListOperations - list cloud service operations",
      "type": "query",
      "executor": "cli",
      "command": "hcloud CTS ListOperations --cli-region={region}"
    },
    {
      "id": "TC-02",
      "name": "ListTrackers - list trackers",
      "type": "query",
      "executor": "cli",
      "command": "hcloud CTS ListTrackers --cli-region={region}"
    },
    {
      "id": "TC-03",
      "name": "ListTraces - query recent traces (limit 5)",
      "type": "query",
      "executor": "cli",
      "command": "hcloud CTS ListTraces --cli-region={region} --trace_type=system --limit=5"
    },
    {
      "id": "TC-04",
      "name": "ListNotifications - list SMN notifications",
      "type": "query",
      "executor": "cli",
      "command": "hcloud CTS ListNotifications --cli-region={region} --notification_type=smn"
    },
    {
      "id": "TC-05",
      "name": "ListTraceResources - list trace resources requires --domain_id",
      "type": "query",
      "executor": "cli",
      "command": "hcloud CTS ListTraceResources --cli-region={region} --domain_id=074c26ae7f0025b10fd7c0159cb576a0"
    },
    {
      "id": "TC-06",
      "name": "CreateTracker syntax check (--help only, no live mutation)",
      "type": "syntax",
      "executor": "cli",
      "command": "hcloud CTS CreateTracker --cli-region={region} --help"
    },
    {
      "id": "TC-07",
      "name": "CreateNotification syntax check (--help only, no live mutation)",
      "type": "syntax",
      "executor": "cli",
      "command": "hcloud CTS CreateNotification --cli-region={region} --help"
    },
    {
      "id": "TC-08",
      "name": "DeleteTracker syntax check (--help only, no live mutation)",
      "type": "syntax",
      "executor": "cli",
      "command": "hcloud CTS DeleteTracker --cli-region={region} --help"
    },
    {
      "id": "TC-09",
      "name": "Negative: ListTraceResources with --project_id must fail (domain_id required)",
      "type": "negative",
      "executor": "cli",
      "command": "hcloud CTS ListTraceResources --cli-region={region} --project_id=383eff43089245eb90ee42bf24fb697b",
      "expected": "fail",
      "note": "Negative case (BUG-003): the command is expected to FAIL with a CLI error (\"The following parameters are required: domain_id\"). A non-zero exit / CLI error is the PASS condition for this case; testers should count expected-failure as pass, not fail."
    },
    {
      "id": "TC-10",
      "name": "Negative: ListTraces with invalid --trace_type must fail",
      "type": "negative",
      "executor": "cli",
      "command": "hcloud CTS ListTraces --cli-region={region} --limit=1 --trace_type=invalid_value",
      "expected": "fail",
      "note": "Negative case (BUG-003): the command is expected to FAIL with a CLI enum-validation error (\"The value of trace_type must match [system|data]\"). A non-zero exit / CLI error is the PASS condition for this case; testers should count expected-failure as pass, not fail."
    }
  ]
}