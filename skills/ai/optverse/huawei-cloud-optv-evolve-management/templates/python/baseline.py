"""
Baseline function template - Python

Constraints:
- Must be a parameterless `def baseline()`; the filename must exactly match
  the value passed to `CreateEvolveTask --evaluator_baseline`
- Return-value semantics must match `evaluate()`
"""


def baseline():
    #  fill in the baseline-version evaluation logic
    score = 0.0
    return score
