# 云原生评估报告

## 1. 云原生技术介绍

  CNCF（Cloud Native Computing Foundation（云原生计算基金会））对云原生定义：云原生技术有利于各组织在公有云、私有云和混合云等新型动态环境中，构建和运行可弹性扩展的应用。云原生的代表技术包括容器、服务网格、微服务、不可变基础设施和声明式API。这些技术能够构建容错性好、易于管理和便于观察的松耦合系统。结合可靠的自动化手段，云原生技术使工程师们能够轻松地对系统作出频繁和可预测的重大变更。

### 1.1 云原生关键技术：容器&不可变基础设施
容器能够将一个包含应用及运行依赖的镜像按照指定资源诉求在物理机或虚机上启动成为一个或一组隔离的OS进程，天然具备敏捷、弹性、可移植的特点。
不可变基础设施（Immutable Infrastructure）是由 Chad Fowler 于2013 年提出的一个很有前瞻性的构想：在这种模式中，任何基础设施的实例（包括服务器、容器等各种软硬件）一旦创建之后便成为一种只读状态，不可对其进行任何更改，如果需要修改需要构建新的实例（服务器、容器），然后替换全部旧的实例。不可变基础设施的优势在于更高的一致性和可靠性，创建、部署、升级的过程是可预测的，给运维带来的好处也很明显：
- (1)	由于每次变更都是使用新版本实例完整的替换旧版本，不依赖当前的运行环境，所以应用实例的升级、回滚更可控。
- (2)	更容易水平扩展。
- (3)	在灾难发生的时候，快速恢复环境。
容器一开始就按照不可变基础设施的理念在设计，容器包括三个基本概念：容器镜像、容器实例、容器仓库。

容器镜像和容器实例一旦创建成功便不能修改，只能替换。容器不可变基础设施的特性使得容器成为云原生时代软件交付的标准模式。

### 1.2 云原生关键技术：声明式API
对于系统的管理有两种方式，一种是声明式一种是命令式。
 
命令式管理指的是运维系统时需要运维人员直接发出运维命令，例如： 运行容器、停止容器等。而声明式管理指的是运维人员在系统中声明最终的状态，而系统根据运维人员声明的状态自动的不断向该状态驱动，最终达到期望的状态。声明式管理的好处是显而易见的，声明式 API 使系统更加健壮，在分布式系统中任何组件随时都可能出现故障，如果所有的故障都需要运维人员介入处理系统的可靠性肯定无法得到保障，此时只有采用声明式管理，系统不需要人工介入即可以自治恢复。
要实现系统的自动声明式管理需要两个条件：通用的状态定义API和智能可扩展的自治处理架构。在云原生技术体系中，实现对容器进行声明式管理和编排的就是Kubernetes。 Kubernetes最主要的设计理念就是声明式API和控制循环（系统自治）。
Kuberentes设计理念—声明式API：
API对象是K8s集群中的管理操作单元。K8s集群系统每支持一项新功能，引入一项新技术，一定会新引入对应的API对象，支持对该功能的管理操作。每个API对象都有3大类属性：元数据metadata、规范spec和状态status：
- 元数据是用来标识API对象的，每个对象都至少有3个元数据：namespace，name和uid。
- 规范描述了用户期望K8s集群中的分布式系统达到的理想状态（Desired State）。
- 状态描述了系统实际当前达到的状态（Status）。   
Kuberentes设计理念—控制循环：
大型分布式系统必然会存在各种异常，比如系统崩溃、容器退出等。Kubernetes 自然会持续关注系统的实时状态，当遇到异常时能够及时的进行自我修复。
- 持续观测、校正，最终将运行状态达到用户期望的状态。
- 感知用户的行为并执行。比如修改 Pod 数量，应用升级/回滚等等。
Kubernetes 实现了大量的 controllers，它们通过 list-watch etcd 来感知集群数据的更新，然后 24 小时不间断的工作以达到期待的状态，在该过程中它们也会把创建的各类数据反馈回 kube-apiserver & etcd，从而形成了数据流的闭环。
Kubernetes 不会基于系统当前状态和期望状态来来确定接下来要执行的一组经过精确计算的相互协调的命令，而是仅基于系统当前状态确定下一个要执行的命令，然后不断迭代，直到没有下一个命令可以执行，系统就达到了稳定状态。
 
Kubernetes架构上由控制面和运行实际业务的数据面（node）组成。
 
Kubernetes 控制面由API Server、Controller、Scheduler 以及 etcd组成。Apiserver作为kubernetes系统的入口，封装了核心对象的增删改查操作，以RESTful接口方式提供给外部客户和内部组件调用。Kubernetes的应用定义如无状态应用Deployment、有状态应用Statefulset等已成为企业抽象不同类型工作负载的的事实标准。Etcd是一个持久的、轻量级的、分布式的、键值数据存储的数据库，它存储了集群的配置信息，并提供了list-watch等高级功能，便于各个控制器很容易获得系统的状态变化，从而快速响应和协调工作。Controller manager 基于控制循环理念负责维护集群的状态，如故障检测、自动扩展、滚动升级等。Scheduler则负资源的调度，按照预定的调度策略将应用调度到相应的节点上。
Kubernetes可扩展性非常强，从一诞生就具备自带平台熟属性：
 
Kubernetes北向提供了容器、集群、服务、应用、配置、租户、网络、存储、策略等等一系列的标准化原子接口，使得多种多样的上层行业场景可以基于自身的应用领域来按需组合使用，并可以通过K8s灵活的CRD（Custom Resource Definitions）机制来面向行业扩展、二次开发相关功能，从而方便地解决企业业务容器化改造、新业务完整技术方案设计、企业流程化方案设计与实施等方面的问题。基于Kubernetes灵活的北向扩展机制，可以将企业内现存的各类基础软件平台，典型的包括以DevOps持续交付、微服务化开发为代表的应用与微服务平台，以AI、大数据、批量计算为代表的数据平台，以及面向5G、物联网、视频等场景的边缘平台，都将会统一到基于Kubernetes的“云原生架构的技术平台”之上。从而，企业基于云原生技术平台将会真正实现对企业内各类业务的统一管理。
 
Kubernetes南向抽象了面向各类运行环境的标准化接口，比如容器运行时的CRI（Container Runtime Interface）、容器网络的CNI（Container Network Interface）、容器存储的CSI（Container Storage Interface），以及各类专用设备与芯片的Device Plugins等，使得企业能够基于自身对性能、安全、高可用、高可靠等关键业务SLA指标的诉求，选取合适的业务运行环境技术体系，将多样化的企业业务运行环境归一到Kubernetes平台统一管理。“应用、平台、设备”三个层面的协同，意味着Kubernetes将真正成为“云原生OS“。

### 1.3 云原生关键技术：微服务
Gartner在2019年12月发表的《将微服务架构原理应用于应用程序架构的解决方案路径》一文中，对微服务和微服务架构有如下定义：“微服务是应用程序组件，它们具有范围狭窄，高度凝聚力，强封装，松散耦合，可独立部署和可独立扩展的特性。它们通过具有定义良好的API合同的开放式标准网络可访问接口作为服务部署。这些组件拥有它们提供的数据，并且仅提供它们拥有的数据。  微服务架构（MSA）是一种设计范例，将SOA和域驱动的设计原理应用于分布式应用程序。MSA具有三个核心目标：开发敏捷性，部署灵活性和精确的可伸缩性。”
软件的最高境界是高内聚、低耦合，符合SOLID原则。但是面对复杂业务系统，采用怎样的方法才能实现系统的高内聚、低耦合呢？微服务就是一种很好的设计理念：微服务试图分离技术实现的复杂性，将复杂的业务逻辑拆解为多个简单逻辑，并通过这些简单逻辑的组合来实现复杂的业务逻辑。通过将庞大、复杂的单体应用分解为合适大小的独立服务的方式来控制业务的复杂性，从而解决软件难以理解、难以演进的难题。从这个意义上讲微服务架构是应用程序的"正确"的下一代体系结构，但微服务不是银弹，为了实现微服务软件的治理需要一台成熟度微服务框架，包括:
 
根据微服务开发模式对开发人员的侵入程度，可以将微服务开发模式分为三代：
第一代服务治理能力内嵌在业务代码中，典型技术包括SOA、ESB等。这种开发模式使用简单，外部依赖少，但是带来了业务代码与服务治理代码代码耦合度高、服务治理代码分散在各个业务模块中，代码重复度高、运维复杂，对开发人员要求也很高。
为了解决第一代开发模式出现的问题，很自然的就出现了以Springcloud、Dubbo为代表的第二代微服务开发模式，即将服务治理能力抽象到统一的SDK实现，业务开发人员基于SDK开发业务代码。这种方式解决了第一代微服务开发代码重复量大、治理逻辑与业务逻辑耦合的问题，但也带来了如下问题：SDK开发语言绑定，无法进行跨语言的统一服务治理，约束了开发语言选型；同时业务逻辑与SDK编译依赖，一旦SDK升级则业务也需要重新编译构建。
 
为了达到服务治理与业务逻辑彻底解耦，将服务治理能力下沉到基础设施的目的，以Istio为代表的服务网格技术应运而生。

### 1.4 云原生关键技术：服务网格
Istio一种云原生的、应用层的、网络技术，用于解决组成应用的组件之间的连接、 安全、策略、可观察性等问题，具有如下特点：
- 多协议（Dubbo、gRPC），多语言（nodejs、.Net、java）兼容遗留老应用成为云原生应用
- 高性能通信，轻量代理。
- 整合容器平台，微服务引擎，API网关，指标监控，日志审计等云上服务，封装为微服务平台，让用户感知不到背后的复杂（服务注册、路由策略、负载均衡、监控数据）
- 可靠的传输复杂网络拓扑中的服务请求，将服务变为现代的云原生服务
从架构上看，Istio是对K8s的完美补充：
- Istio在K8s已有的服务注册、配置、发现基础上增强了服务调用、安全、链路监控、流量治理等微服务关键功能。
- 满足容器应用的部署、伸缩、运维、服务发现等运行诉求。
 
软件架构
 
client端访问backend的所有请求，都通过iptable规则将流量劫持到sidecar容器envoy中处理。这两个访问间的服务发现、熔断、限流、访问控制等均通过Kubernetes CRD的方式在控制面配置。
 
核心理念：
- 非侵入式Sidecar注入技术，将数据面组件注入到应用所在的容器，通过劫持应用流量来进行功能实现，应用无感知。
- 北向API基于K8s CRD实现，完全声明式，标准化。
- 数据面与控制面通过xDS gRPC标准化协议通信，支持订阅模式。
核心特性：
- 服务&流量治理：熔断，故障注入，丰富的负载均衡算法，限流，健康检查，灰度发布，蓝绿部署等。
- 流量与访问可视化：提供应用级别的监控，分布式调用链，访问日志等。
- 安全连接：通过mTLS、认证、鉴权等安全措施帮助企业在零信任的网络中运行应用。
主要应用场景：
- 为应用提供无侵入式流量治理能力。
- 多K8s集群场景提供跨集群流量统一管理能力。




## 2. 云原生设计方法

### 2.1 使用容器交付支撑频繁可控的部署升级
  在云原生架构中，容器交付不仅是应用打包的标准格式，更是实现业务频繁迭代与平滑升级的核心载体。通过构建基于容器的标准化交付流水线，企业能够将应用及其运行环境封装为不可变的镜像制品，彻底消除“开发、测试、生产环境不一致”带来的部署风险，从而支撑起高频次、高可靠性的版本更新。

### 2.2 充分使用云的能力帮助应用实现无状态设计

  软件中服务所有基础组件要求遵循数据与应用分离（服务无状态化），可按需水平扩展；保证在业务量增加，用户增加，数据量增长的情况下能够通过扩容手段保证服务质量。
  软件的业务负载可以通过负载均衡的方式进行分布式的业务负载分发，从而实现服务的弹性伸缩，每个服务具备横向扩展能力，支持按需使用、自动弹性伸缩，可动态替换、灵活部署，支撑高性能、高吞吐量、高并发、高可用业务场景。 
  应用无状态设计实现的难点在于对应用状态的处理，任何状态需要考虑它的分发、处理、存储。应用无状态化的总体思路就是将这些数据存储到后端有状态的中间件中，如分布式数据库、分布式缓存、分布式消息、对象存储、Nosql数据库、文件存储等，这些中间件从设计之初就考虑了自身的弹性、同步等能力，不需要应用层关心。最终系统通过无状态的应用加成熟的中间件实现整体的SLA。
  对于数据的存储，主要分为以下几类数据，应用可以根据建议选择合适的处理方法：
- 内存数据，如Session信息：统一存放到分布式缓存中间件中。
- 应用业务数据：存储到外部数据库中。
- 文件、照片等数据：存储到SFS或对象存储中，并使用CDN预加载。
- 非结构化数：ElasticSearch、mongodb中。
- 应用配置：配置中心，如Kubernetes ConfigMap或Secret。

### 2.3 使用微服务管理业务复杂性

  在传统单体架构中，随着业务规模扩大和功能迭代，代码耦合度会持续升高，任何一个模块的修改都可能引发 “牵一发而动全身” 的风险，不仅开发效率低下，故障排查和系统扩展也变得异常困难 —— 这正是业务复杂性积累到一定阶段的典型痛点。而微服务架构通过 “分而治之” 的核心思想，将复杂的业务系统拆解为一系列独立、可自治的小型服务，每个服务仅聚焦于特定的业务领域（如电商系统中的用户服务、订单服务、支付服务），服务间通过标准化的接口（如 REST、gRPC）进行通信，从根本上解决了单体架构的耦合问题。
  拆分后的微服务具备极强的灵活性，每个服务可以由独立的团队负责开发、测试、部署和维护，团队能够根据业务需求选择最适合的技术栈（如订单服务用 Java 保障稳定性，推荐服务用 Python 提升算法迭代效率），实现 “技术栈解耦”。同时，当某一业务模块需要扩展时（如促销活动期间订单量激增），只需针对性地对订单服务进行水平扩容，无需对整个系统进行升级，既降低了资源浪费，又提升了扩展的精准性。此外，微服务的独立部署特性使得迭代速度大幅提升，单个服务的更新可以在不影响其他服务的前提下完成，缩短了业务功能从开发到上线的周期，让系统能够快速响应市场变化。
  不过，微服务在降低业务复杂性的同时，也引入了分布式系统的管理挑战，如服务注册与发现、负载均衡、分布式事务、链路追踪等问题。因此，在采用微服务管理业务复杂性时，需要配套构建完善的服务治理体系：通过服务注册中心（如 Nacos、Eureka）实现服务的动态发现，借助 API 网关（如 Gateway、Kong）统一管理服务入口，利用分布式事务框架（如 Seata）解决跨服务数据一致性问题，再通过链路追踪工具（如 SkyWalking、Jaeger）实现故障的快速定位。只有将 “拆分” 与 “治理” 相结合，才能真正发挥微服务在管理业务复杂性中的价值，让系统在复杂业务场景下既保持灵活迭代，又具备稳定运行的能力。

### 2.4 面向故障设计，提升业务可靠性

  在云原生环境中，故障是必然发生的常态 —— 服务器硬件损坏、网络链路中断、依赖服务超时、流量突发峰值等问题随时可能出现。传统架构往往以 “规避故障” 为目标，通过采购高可用硬件、优化网络环境等方式降低故障概率，但这种被动防御的思路难以应对云环境的动态不确定性。而 “面向故障设计” 则转变为主动策略，核心是承认故障的必然性，通过提前预设故障场景、构建 “故障韧性”，让系统在故障发生时能够快速恢复，甚至在部分组件故障的情况下仍能保障核心业务的正常运行，最终提升整体业务可靠性 。
  实现面向故障设计的核心手段包括冗余设计、限流熔断与降级、故障注入演练三大方向。冗余设计是基础，通过在多可用区、多地域部署服务实例，确保单一节点或区域故障时，其他节点能立即接管业务 —— 例如，将应用服务器和数据库分别部署在三个不同可用区，当其中一个可用区因断电中断时，负载均衡器会自动将流量转发至正常可用区，实现 “无感知切换”。限流与熔断则聚焦于 “防止故障扩散”：限流通过设定接口的最大并发量（如每秒处理 1000 个请求），避免突发流量冲垮服务；熔断则在服务调用持续失败（如失败率超过 50%）时，暂时切断调用链路，返回预设的兜底响应（如 “服务暂时繁忙，请稍后重试”），防止故障从依赖服务传导至核心服务，保障系统 “不雪崩”。
  故障注入演练则是验证系统韧性的关键环节。通过主动向系统注入故障（如模拟服务器宕机、网络延迟、数据库读写失败等），观察系统的响应和恢复过程，发现潜在的设计缺陷 —— 例如，电商平台在大促前通过故障注入工具关停 30% 的订单服务实例，测试剩余实例是否能通过负载均衡承接流量，以及降级策略是否能保障用户 “下单” 核心功能。此外，完善的监控与自愈能力也是面向故障设计的重要支撑：通过全链路监控工具实时采集服务的 CPU 使用率、响应时间、错误率等指标，一旦发现异常（如错误率突升），自动触发告警并执行自愈操作（如重启服务、扩容实例），将故障对业务的影响降至最低。

### 2.5 通过 CI/CD 实施DevOps 和自动化
  DevOps 的核心目标是打破开发（Dev）、测试（Test）、运维（Ops）团队之间的壁垒，实现 “从代码提交到业务上线” 的高效协作与流程自动化，而 CI/CD（持续集成 / 持续交付 / 持续部署）正是落地 DevOps 的核心工具链与流程载体。CI（持续集成）聚焦于 “开发阶段的协作与质量保障”，要求开发人员频繁将代码提交至共享代码仓库（如 GitLab、GitHub），通过自动化工具（如 Jenkins、GitLab CI）触发构建、编译、单元测试、代码质量扫描（如 SonarQube）等流程，确保每一次代码提交都能快速验证可行性 —— 例如，当开发人员提交一段订单模块的代码后，CI 流程会自动编译代码，运行预设的单元测试用例，若测试失败或代码存在漏洞，会立即向团队反馈，避免问题代码进入后续环节，从源头减少集成风险。
  CD 则分为持续交付（Continuous Delivery）和持续部署（Continuous Deployment），前者聚焦于 “将代码安全、快速地交付至生产环境就绪状态”，后者则实现 “交付后的自动化部署”。在持续交付阶段，经过 CI 验证的代码会被自动打包为镜像（如 Docker 镜像），推送至镜像仓库（如 Harbor），随后触发自动化集成测试、性能测试（如 JMeter）、安全扫描（如 Trivy）等流程，验证镜像在模拟生产环境中的可用性；所有测试通过后，镜像会被标记为 “可部署状态”，等待人工审批后即可部署至生产环境。而持续部署则在此基础上进一步自动化，当镜像通过所有验证后，无需人工干预，通过部署工具（如 ArgoCD、Flux）自动将镜像部署至生产环境的 Kubernetes 集群，实现 “代码提交后，几小时内完成上线” 的高效迭代。
  通过 CI/CD 实施 DevOps 和自动化，不仅能大幅提升研发效率，更能保障业务的稳定性与可追溯性。一方面，自动化流程替代了传统的人工编译、测试、部署等重复性工作，将研发团队从繁琐的操作中解放出来，专注于业务逻辑开发，同时避免了人工操作可能出现的失误（如部署时漏传配置文件）。另一方面，CI/CD 流程通过日志记录每一个环节的操作（如谁提交了代码、哪次测试失败、何时完成部署），实现了全流程可追溯，一旦生产环境出现问题，能够快速定位到具体的代码版本和部署环节，缩短故障排查时间。此外，自动化的质量与安全检测，让代码在进入生产环境前就经过多轮验证，降低了线上故障的概率，最终实现 “快速迭代” 与 “稳定运行” 的平衡，这正是云原生环境下业务持续发展的核心需求。

## 3. 现状分析

### 3.1 被扫描对象信息

| 项目 | 内容 |
|------|------|
| 扫描对象 | 待补充 |
| 扫描时间 | 待补充 |
| 集群规模 | 待补充 |
| 应用数量 | 待补充 |

### 3.2 云原生评估分析详情

#### 3.2.1 服务化

##### 评估项1：容器化覆盖率

| 评估项描述 | 所有业务以容器镜像的方式发布并运行在容器中 |
|-----------|-------------|
| 量化方式 | 100%应用完成容器化 |
| 验收方式 | 检查 K8s 部署服务数 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项2：镜像符合最佳实践

| 评估项描述 | 减小镜像体积，以加速传输、部署速率 |
|-----------|-------------|
| 量化方式 | 镜像大小≤500M（对于基础镜像过大的兜底方案） |
| 验收方式 | 镜像统计工具/镜像仓库能力 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项3：镜像符合最佳实践

| 评估项描述 | 将构建环境和运行环境分离，在最终镜像中仅保留运行必需的产物，从而优化镜像大小。 |
|-----------|-------------|
| 量化方式 | 镜像采用多段build |
| 验收方式 | DockerFile中使用多段build |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项4：镜像符合最佳实践

| 评估项描述 | 避免因更新包版本导致构建结果的不确定性，防止引入新的依赖或安全风险。在构建时不确定哪些包会被安装，可能会产生不一致的镜像和引入未知的依赖。 |
|-----------|-------------|
| 量化方式 | 不要在镜像构建中升级版本 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项5：镜像符合最佳实践

| 评估项描述 | 将变化频率一样的指令（例如RUN）合并在一起，利用Docker的构建缓存机制，减少重复构建，提升构建效率，避免带来不必要的成本。 |
|-----------|-------------|
| 量化方式 | 将变化频率一样的RUN指令合一 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项6：镜像符合最佳实践

| 评估项描述 | 将变化最少的部分放在Dockerfile的前面，通过将变化频率低的步骤前置，充分利用构建缓存，从而加速镜像构建过程。 |
|-----------|-------------|
| 量化方式 | 优先使用COPY,合理调整COPY与RUN的顺序 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项7：镜像符合最佳实践

| 评估项描述 | 确保应用程序进程成为PID 1，使其能正确接收和处理操作系统信号（如SIGTERM），实现优雅终止。 |
|-----------|-------------|
| 量化方式 | 在entrypoint脚本中使用exec, 禁止使用sh启动 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项8：镜像符合最佳实践

| 评估项描述 | 清理构建过程中产生的临时文件和缓存，以减小最终镜像体积并降低安全风险。 |
|-----------|-------------|
| 量化方式 | 删除多余文件 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项9：镜像符合最佳实践

| 评估项描述 | 给镜像添加标签来帮助组织镜像、记录许可信息、辅助自动化构建。 |
|-----------|-------------|
| 量化方式 | 使用特定的标签 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项10：维护并使用标准基础镜像

| 评估项描述 | 所有镜像基于统一的安全基础镜像，确保环境的一致性、提高效率与可靠性、提升安全性（集中管理和打补丁）并降低维护成本（无需每个应用团队各自维护和优化基础镜像）。标准化的镜像减少了构建过程中的不确定性，加速构建流程，并因其经过充分测试而更具可靠性。 |
|-----------|-------------|
| 量化方式 | 所有镜像基于统一的安全基础镜像 |
| 验收方式 | DockerFile检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项11：容器服务单一性

| 评估项描述 | 一个容器只运行一个进程。复杂容器的构建时间非常长、镜像体积大、横向扩展时非常浪费资源（不同的应用需要运行的容器数并不相同）、个进程会混合日志输出难以追踪和诊断、进程崩溃可能影响多个进程。一个容器只运行一个进程遵循单一职责原则，确保容器的高内聚、低耦合，从而实现高效的编排、故障隔离、资源管理和可观测性。 |
|-----------|-------------|
| 量化方式 | 一个容器只运行一个进程 |
| 验收方式 | DockerFile和部署模版中指定的启动脚本指启动单一进程 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项12：使用Deployments 而不直接使用Pod

| 评估项描述 | 避免业务直接创建Pod。无状态服务使用Deployment实现便捷的滚动更新、回滚以及在故障时自愈（如节点失败后重建Pod），支持应用服务的高可用性和弹性。 |
|-----------|-------------|
| 量化方式 | 无业务使用直接创建Pod的模版 |
| 验收方式 | 检查业务部署模板 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项13：Java应用支持根据容器资源配置适配JVM

| 评估项描述 | 容器中JVM需“感知”并充分利用容器分配的资源限额内存，避免因资源视图不一致而导致的内存溢出（OOM Killer杀进程）或资源浪费（容器内存提升JVM内存无法提升） |
|-----------|-------------|
| 量化方式 | JVM参数中配置合理的堆内存大小 |
| 验收方式 | java版本小于8u191，使用Xms、Xmx方式配置堆内存大小，脚本中主动设置Xms、Xmx与容器内存配额的比例<br>java版本大于等于8u191，使用-XX:MinRAMPercentage、-XX:MaxRAMPercentage配置当总内存小于1G建议MaxRAMPercentage不超过50，大于2G则可以配置为75，不建议超过75 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项15：服务镜像一致性（IaC）

| 评估项描述 | 开发环境、测试环境、 生产环境业务使用相同构建文件，保持镜像一致性，确保从开发、测试到生产的整个软件交付流程中，应用运行环境完全一致，从而彻底消除“环境差异”导致的不可预测行为，实现部署的可靠性和可重复性。 |
|-----------|-------------|
| 量化方式 | 开发环境、测试环境、 生产环境业务镜像保持一致性。 |
| 验收方式 | 检查CI |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项17：自动注册

| 评估项描述 | 需要服务注册管理的服务，服务Pod启动且服务准备就绪后自动注册地址到注册中心从而及时暴露服务 |
|-----------|-------------|
| 量化方式 | 需要服务注册管理的服务，服务Pod启动且服务准备就绪后自动注册地址到注册中心 |
| 验收方式 | 代码检查<br>注册中心检查 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项22：Deployment类型进行无状态化改造

| 评估项描述 | 无状态的应用支持多副本高可用部署。每个服务具备横向扩展能力，支持按需使用、自动弹性伸缩，支撑高性能、高吞吐量、高并发、高可用业务场景。 |
|-----------|-------------|
| 量化方式 | 业务多副本部署（副本数≥2 ）<br>多副本同时工作 |
| 验收方式 | 1. 部署模版中副本数≥2<br>2. 不同副本能处理业务请求或有日志证明应用处于工作状态 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项23：Deployment存储依赖无状态化改造

| 评估项描述 | 避免业务依赖本地存储导致无法快速扩缩容。遵循数据与应用分离（服务无状态化），可按需水平扩展；保证在业务量增加，用户增加，数据量增长的情况下能够通过扩容手段保证服务质量。 |
|-----------|-------------|
| 量化方式 | 多副本部署采用分布式存储，无本地存储依赖 |
| 验收方式 | 1.部署模版中主容器无本地磁盘挂载，禁止使用hostpath挂载本地目录。共享内存数据，如Session信息，统一存放到分布式缓存中间件中<br>2. 业务数据存储到外部数据库中。文件存储到共享文件存储或对象存储中 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

#### 3.2.2 韧性

##### 评估项14：配置资源限制

| 评估项描述 |  |
|-----------|-------------|
| 量化方式 | 部署模版100%配置requests/limits。 根据业务类型不同设置不同的资源超分比例 |
| 验收方式 | 检查部署文件，所有负载配置资源请求<br>核心任务request:limit配置 1:1稳定优先，性能优越。<br>关键应用request:limit配置 1:1.5~1:2 稳定相对优先，性能有保障。<br>普通任务request:limit配置 1:2~1:3 稳定，性能通常有保障，节点资源利用率高于60%时，受影响<br>短任务request:limit配置 不配置~1:5 优先级最低，节点资源利用率高时，可以驱逐这部分业务 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项16：容器启动时间

| 评估项描述 | 控制容器启动时间 30 秒 （java应用≤90秒），使得业务在故障恢复或弹性扩容时能快速拉起 |
|-----------|-------------|
| 量化方式 | ≤ 30 秒 （java应用≤90秒） |
| 验收方式 | 查看容器日志启动时长<br>CICD检查启动时间 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项18：容器支持优雅退出

| 评估项描述 | 应用应支持优雅退出。容器被终止时，让其内部进程有机会完成必要的清理工作（如处理完已接收的请求、释放资源、保存状态、 服务注销），从而保证数据一致性并实现业务流量的无损或平滑下线，避免强制中断导致的错误和数据损坏。 |
|-----------|-------------|
| 量化方式 | 应用100%支持优雅退出 |
| 验收方式 | 1.业务证明有处理终止信号 <br>2.部署脚本规范启动命令（禁止使用sh启动）<br>3. 存在服务注册的业务在Pod终止时主动注销注册 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项19：健康检查和故障自愈

| 评估项描述 | 配置 Liveness 和 Readiness 探针让 Kubernetes 能够自动判断应用的健康状态，从而支持容器故障自愈和服务发现 |
|-----------|-------------|
| 量化方式 | 负载探针覆盖率 100% |
| 验收方式 | 1.检查部署文件配置探针 <br>2.应用提供业务健康状态检查API，例如 /health |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项21：无NodePort强绑定

| 评估项描述 | 禁止使用Nodeport对外暴露服务， 节点故障、重启或缩容后可能影响业务访问 |
|-----------|-------------|
| 量化方式 | 禁止使用Nodeport对外暴露服务 |
| 验收方式 | 检查部署模版 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项24：服务跨可用区高可用

| 评估项描述 | 通过亲和策略将业务部署到不同的可用区中以提高可用性，避免因可用区区故障导致服务终端。有极致通信速率要求的业务可考虑同可用区部署以提高性能。 |
|-----------|-------------|
| 量化方式 | 重要应用多副本数≥2 + 反亲和性配置 + AZ拓扑约束配置 |
| 验收方式 | 检查部署模版中采用Pod反亲和尽量将Pod分散在不同节点<br>检查部署模版中配置跨AZ拓扑约束将Pod均匀分散在不同AZ |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项25：集群管理面高可用

| 评估项描述 | 集群管理面采用多master（3个以上）高可用部署，消除单点故障从而实现生产级的高可靠性 |
|-----------|-------------|
| 量化方式 | 至少需要3个管理节点 |
| 验收方式 | 检查集群管理面配置 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项26：禁止生产环境所有集群的节点分布在同一AZ，必须跨AZ分布

| 评估项描述 | 集群节点分布在不同可用区，提高节点可靠性 |
|-----------|-------------|
| 量化方式 | 验证生产集群节点创建在不同AZ中 |
| 验收方式 | 获取nodepool所有节点信息，至少有2个不同AZ |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项27：业务按节点池调度资源隔离，保障性能和可靠性

| 评估项描述 | 根据工作负载的特性和资源需求，通过亲和调度方式将业务按节点池调度，减少业务间的资源竞争和相互干扰，保障业务性能和稳定性 |
|-----------|-------------|
| 量化方式 | 配置节点亲和策略按节点池调度 |
| 验收方式 | 检查部署模版中采用NodeSelector、NodeAffinity调度 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项40：合理选择集群大小

| 评估项描述 | 根据业务类型和业务规模合理选择集群大小。运行长稳业务的集群规模不超过250节点，运行大数据等job类应用集群的规模不超过2000节点。开发环境和测试环境可使用大规模集群以降低管理资源消耗。 |
|-----------|-------------|
| 量化方式 | 运行长稳业务的集群规模不超过250节点，运行大数据等job类应用集群的规模不超过2000节点。<br>开发环境和测试环境可使用大规模集群以降低管理资源消耗。 |
| 验收方式 | 获取集群节点数量，对比建议规格 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

#### 3.2.3 弹性

##### 评估项20：应用支持实例IP随机可变

| 评估项描述 | 容器IP为非固定IP，业务重新部署后IP可能变化。业务不应使用固定IP进行业务互访 |
|-----------|-------------|
| 量化方式 | 应用无内部IP地址硬编码 |
| 验收方式 | 代码扫描 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项28：支持业务定时扩缩容

| 评估项描述 | 对于流量有周期规律的业务， 根据可预测的业务流量高峰与低谷规律，预先增加或减少 Pod 副本数，从而在保障服务性能的同时，显著优化成本。 |
|-----------|-------------|
| 量化方式 | 配置cron HPA，定时自动扩缩容业务 |
| 验收方式 | 检查集群HPA配置 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项29：支持集群动态扩缩容

| 评估项描述 | 集群定时或按照节点资源利用率自动扩缩容集群节点数量以满足业务资源需求 |
|-----------|-------------|
| 量化方式 | 配置ClusterAutoscaler定时或按照节点资源利用率自动扩缩容集群资源 |
| 验收方式 | 检查集群CA配置 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

#### 3.2.4 可观测性

##### 评估项30：接口监控指标覆盖率

| 评估项描述 | 提供集群和业务的监控指标，将系统的运行状态转化为可量化、可分析的时间序列数据，帮助洞察系统、业务健康状态、快速定位问题并优化资源效率 |
|-----------|-------------|
| 量化方式 | 基础关键指标数≥10<br>（必须有，数量待参考；业务基于监控源数据建立自己的关键指标大盘） |
| 验收方式 | 监控指标看板<br>基础： 节点CPU，节点内存，节点磁盘IO，节点网络带宽，Pod数量，Pod重启数，Pod状态，Pod CPU，Pod内存<br><br>应用：通过/metrics api提供标准指标，包括jvm指标，关键api响应时间， api错误率（成功率）， api QPS。 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项31：日志标准化输出

| 评估项描述 | 日志输出到stdout/stderr，统一日志收集行为，解耦日志与业务逻辑，将日志的存储，轮转和收集交于基础设施负责，提升日志的可收集性。 |
|-----------|-------------|
| 量化方式 | 100%日志按照标准格式输出到stdout/stderr |
| 验收方式 | 查看Pod日志或日志平台检查服务日志<br>1. 服务日志stdout/stderr输出 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项32：日志标准化输出

| 评估项描述 | 采用JSON格式输出，并附带trace id, app id等上下文信息，提升了日志的可解析性和可观测性 |
|-----------|-------------|
| 量化方式 | 日志格式规范化 |
| 验收方式 | 查看Pod日志或日志平台检查服务日志<br>2. 日志以JSON格式输出（直接看日志元数据不友好）<br>3. 遵守日志规范：API日志携带API ID，上下文等 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项33：持久化日志并支持查询

| 评估项描述 | 基础设施支持收集、存储日志以及日志查询 |
|-----------|-------------|
| 量化方式 | 支持收集、存储日志<br>支持日志查询 |
| 验收方式 | 查看CCE配置信息，是否开启日志插件kube-prometheus-stack和CCE Log Collector |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项34：告警配置

| 评估项描述 | 通过持续监控系统关键指标并在异常达到阈值时主动通知，为运维和业务团队提供故障的即时感知与定位能力 |
|-----------|-------------|
| 量化方式 | 集群配置基础资源和业务状态告警 |
| 验收方式 | 1. 检查告警规则是否有配置<br>2. 检查是否有配置告警通知 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项35：支持记录集群资源操作审计日志

| 评估项描述 | 对集群内所有关键操作（如谁、在什么时间、对什么资源、执行了什么动作）进行不可篡改的追踪和记录，从而满足安全合规、故障排查与责任追溯的需求 |
|-----------|-------------|
| 量化方式 | 集群收集并记录非查询了操作审计日志 |
| 验收方式 | 查看CCE配置信息，是否开启日志插件kube-prometheus-stack和CCE Log Collector |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

#### 3.2.5 安全性

##### 评估项36：配置pod和容器安全上下文

| 评估项描述 | 配置Pod安全上下文遵循“最小权限原则”，从内核层面限制容器和 Pod 的权限与能力，从而最大限度地降低容器被恶意利用时的攻击面，增强工作负载的隔离性与安全性。在镜像构建时创建非root用户并设置对应的文件权限 |
|-----------|-------------|
| 量化方式 | 包括单不限于以下配置<br>1. 所有负载通过RunAsUser指定non-root用户<br>2. 非必要避免使用特权模式 |
| 验收方式 | 检查部署文件 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项37：安全事件数量

| 评估项描述 | 业务发布前进行代码、镜像、开源组件的安全扫描，确保无重大安全漏洞 |
|-----------|-------------|
| 量化方式 | 无 P0 漏洞 |
| 验收方式 | 安全扫描报告<br>1. 代码仓扫描无漏洞报警<br>2. 镜像扫描无漏洞报警<br>3. 扫描开源组件无漏洞报警 |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项38：容器安全配置

| 评估项描述 | 确保业务部署模版未挂载敏感目录或共享系统命名空间，降低容器被恶意利用时的攻击面，增强工作负载的隔离性与安全性<br>• 如非必须，不建议将宿主机的敏感目录挂载到容器中，如/、/boot、/dev、/etc、/lib、/proc、/sys、/usr等目录<br>• 如非必须，不建议容器与宿主机共享网络命名空间<br>• 如非必须，不建议容器与宿主机共享进程命名空间<br>• 如非必须，不建议容器与宿主机共享IPC命名空间<br>• 如非必须，不建议容器与宿主机共享UTS命名空间 |
|-----------|-------------|
| 量化方式 | 验收所有业务部署模版未挂载敏感目录或共享系统命名空间<br>• 如非必须，不建议将宿主机的敏感目录挂载到容器中，如/、/boot、/dev、/etc、/lib、/proc、/sys、/usr等目录<br>• 如非必须，不建议容器与宿主机共享网络命名空间<br>• 如非必须，不建议容器与宿主机共享进程命名空间<br>• 如非必须，不建议容器与宿主机共享IPC命名空间<br>• 如非必须，不建议容器与宿主机共享UTS命名空间 |
| 验收方式 | 如非必须，不建议将宿主机的敏感目录挂载到容器中，如/、/boot、/dev、/etc、/lib、/proc、/sys、/usr等目录<br>部署模版中未指定spec.hostIPC，spec.hostNetwork，spec.hostPID |
| 级别 | 要求 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项39：使用最新K8s版本

| 评估项描述 | 最新版本的集群修复了已知的漏洞或者拥有更完善的安全防护机制，推荐使用最新版本的集群。 |
|-----------|-------------|
| 量化方式 | 验证集群版本为最新发行版本（至少高于EOS版本） |
| 验收方式 | 获取集群版本，参考华为云官网信息https://support.huaweicloud.com/intl/zh-cn/bulletin-cce/cce_bulletin_0033.html，集群版本高于EOS版本 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项41：启用RBAC

| 评估项描述 | 通过RBAC授权机制实现最小权限和安全管理，从而有效防止未经授权的访问或误操作，保障集群与应用的安全隔离与合规性。1. 业务仅使用必要的K8s API访问；2. 通过RBAC控制所有服务访问权限；3. 通过RBAC限制所有角色集群访问权限 |
|-----------|-------------|
| 量化方式 | 1. 业务仅使用必要的K8s API访问 <br>2. 通过RBAC控制所有服务访问权限<br>3. 通过RBAC限制所有角色反问权限 |
| 验收方式 | 检查集群配置和业务模版serviveaccount配置 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项42：Ingress 配置TLS安全访问

| 评估项描述 | 为Ingress配置TLS，为进入集群的外部HTTP(S)流量提供端到端的加密与身份验证，保障数据传输的机密性、完整性，并满足安全合规要求。 |
|-----------|-------------|
| 量化方式 | 验证业务ingress配置tls安全访问 |
| 验收方式 | 检查集群ingress配置 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

##### 评估项43：限制node节点外部访问

| 评估项描述 | 安全组规则加固 |
|-----------|-------------|
| 量化方式 | 安全组规则加固 |
| 验收方式 | 检查集群安全组配置 |
| 级别 | 建议 |
| 得分 | /3 |
| 现状分析 | 待补充 |

### 3.3 成熟度评估

#### 3.3.1 综合评分

| 维度 | 得分 | 级别 |
|------|------|------|
| 服务化 | /36 | 待评估 |
| 韧性 | /36 | 待评估 |
| 弹性 | /15 | 待评估 |
| 自动化 | /6 | 待评估 |
| 可观测性 | /24 | 待评估 |
| 安全性 | /24 | 待评估 |
| **总计** | **/141** | **待评估** |

#### 3.3.2 各维度评估结果

> 各维度的详细评估结果和存在的问题...

#### 3.3.3 雷达图

#### 3.3.4 成熟度阶梯图 

---

**报告生成时间：** 待补充
**报告版本：** v1.0
